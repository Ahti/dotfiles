set mouse
set ignorecase
set expand-artists
set singlequit
set windows library,playlist,help
set window library

alias updatenas !mpc -h nas.local update
alias clear deleteall

alias randomall clear; library; addall; random on; play; next

alias ra randomall

map q :q<Enter>
map <C-r> :reconnect<Enter>
map <BS> <Nop>
